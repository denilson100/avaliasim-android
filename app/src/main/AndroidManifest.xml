<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.mobile10.avaliasim">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.Main4Activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.ResetActivity" />
        <activity android:name=".auth.EmailPasswordActivity" />
        <activity android:name=".activity.NewAvaliacao" />
        <activity android:name=".activity.AvaliacaActivity" />

        <activity android:name=".activity.Feature1Activity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".activity.MainIntroPermission"
            android:theme="@style/Theme.Intro"/>

        <!-- Coletar dados analitcs -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="true" />
        <meta-data
            android:name="firebase_crash_collection_enabled"
            android:value="true" />

        <activity
            android:name=".activity.DetalhesAvaliacao"
            android:label="@string/title_activity_detalhes_avaliacao2"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>